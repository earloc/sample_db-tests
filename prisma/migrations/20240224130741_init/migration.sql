BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[FooBars] (
    [id] INT NOT NULL,
    [value] NVARCHAR(1000) NOT NULL,
    CONSTRAINT [FooBars_pkey] PRIMARY KEY CLUSTERED ([id])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
